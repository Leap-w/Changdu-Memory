# V5.3 Phase 1 — 修复报告

## 构建结果

✅ TypeScript: 无错误
✅ Vite build: 成功

---

## 一、大事记功能修复 ✅

### 修改内容
- 新增 `memory_photos` 表类型定义
- 新增 `MemoryPhotoRepository.ts`：uploadMemoryPhoto / fetchMemoryPhotos / fetchMemoryPhotosBatch / deleteMemoryPhoto / deleteAllMemoryPhotos
- 修改 `MemoryRepository.ts`：删除大事记时先清理关联图片
- 修改 `stores/memory.ts`：支持照片缓存管理（getPhotosForMemory / addPhotosToMemory / removePhotoFromMemory）
- 重写 `MemoryTimeline.vue`：图片链接输入 → 文件上传区域，支持预览、上传、删除

### 修改文件
- `src/types/database.ts`
- `src/repositories/MemoryPhotoRepository.ts` (新建)
- `src/repositories/MemoryRepository.ts`
- `src/stores/memory.ts`
- `src/pages/MemoryTimeline.vue`

### 上传路径
`{userId}/memory/{memoryId}/{timestamp}.{ext}`

---

## 二、头像上传修复 ✅

### 修改内容
- 头像路径添加时间戳避免缓存：`{userId}/avatars/{userId}_{timestamp}.{ext}`
- 公开URL添加 `?t=` 缓存破坏参数
- AppAvatar 增加 `imgError` 状态，图片加载失败时显示文字首字母回退

### 修改文件
- `src/pages/Profile.vue`
- `src/components/ui/AppAvatar.vue`

---

## 三、待办事项重新整理 ✅

### 修改内容
- `database.ts`：todos 表新增 `deadline_date` / `deadline_time` 字段
- `TodoRepository.ts`：createTodo / updateTodo 支持新字段
- `stores/todo.ts`：addTodo / editTodo 接口更新
- `TodoEditor.vue`：移除 priority / category 选择器，新增截止日期、截止时间
- `TodoEdit.vue`：适配新字段
- `TodoCard.vue`：简化显示，展示截止时间
- `Work.vue`：待办Tab 移除 priority / category 显示，改为截止时间

### 修改文件
- `src/types/database.ts`
- `src/repositories/TodoRepository.ts`
- `src/stores/todo.ts`
- `src/components/todo/TodoEditor.vue`
- `src/pages/TodoEdit.vue`
- `src/components/todo/TodoCard.vue`
- `src/pages/Work.vue`

### 保留字段
`title` / `description` / `deadline_date` / `deadline_time` / `completed`

### 废弃字段（兼容）
`priority` → 默认 'medium'
`category` → 默认 'life'

---

## 四、行政安排修复 ✅

### 修改内容
- `WorkEditor.vue`：移除 period / category / time，只保留 title / content / date
- `WorkRepository.ts`：createWork / updateWork 接受简化字段
- `stores/work.ts`：接口更新
- `WorkEdit.vue`：简化表单，保存后跳转 `/work?tab=行政安排`
- `Work.vue`：读取 `?tab=` query 参数定位标签页，新增内联删除按钮

### 修改文件
- `src/components/work/WorkEditor.vue`
- `src/repositories/WorkRepository.ts`
- `src/stores/work.ts`
- `src/pages/WorkEdit.vue`
- `src/pages/Work.vue`
- `src/components/work/WorkCard.vue`

---

## 五、课程表优化 ✅

### 修改内容
- `ScheduleWeekView.vue`：完全重写为固定时间轴布局，左侧固定时间段，右侧每天一列，每行固定高度
- `ScheduleEditor.vue`：课程名称默认"政治"，第一输入改为班级名称，新增节次快捷选择按钮

### 显示优先级
1. 班级名称
2. 上课时间
3. 备注信息

### 修改文件
- `src/components/schedule/ScheduleWeekView.vue`
- `src/components/schedule/ScheduleEditor.vue`

---

## 六、资产和福利布局调整 ✅

### 修改内容
- 资产合计行 → 移至资产列表上方
- 福利估值合计行 → 移至福利列表上方

### 修改文件
- `src/pages/Expense.vue`

---

## 七、首页快捷入口调整 ✅

### 修改内容
- 合并"支出记录""收入记录"为"账本"
- 新增"学生""数据管理""回收站"入口

### 最终快捷入口（9个）
日记 · 工作 · 待办 · 账本 · 学生 · 大事记 · 时光中心 · 数据管理 · 回收站

### 修改文件
- `src/pages/Home.vue`

---

## 八、移动端UI调整 ✅

### 8.1 "我的"页面
- groupMain 调整为：大事记 · 时光中心 · 账本
- 新增对应图标样式

### 8.2 底部导航
- emoji 图标替换为 SVG 图标
- 未选中：线性线条风格
- 选中：填充颜色

### 修改文件
- `src/pages/Profile.vue`
- `src/layouts/MobileLayout.vue`

---

## 额外修复

### Import.vue
- 移除 work 导入中的 `period` 和 `category` 参数

### MemoryTimeline.vue
- 移除未使用的变量 `pi`

---

## 涉及文件总览

| 文件 | 操作 |
|------|------|
| `src/types/database.ts` | 修改 |
| `src/repositories/MemoryPhotoRepository.ts` | 新建 |
| `src/repositories/MemoryRepository.ts` | 修改 |
| `src/repositories/TodoRepository.ts` | 修改 |
| `src/repositories/WorkRepository.ts` | 修改 |
| `src/stores/memory.ts` | 修改 |
| `src/stores/todo.ts` | 修改 |
| `src/stores/work.ts` | 修改 |
| `src/pages/MemoryTimeline.vue` | 重写 |
| `src/pages/Profile.vue` | 修改 |
| `src/pages/Home.vue` | 修改 |
| `src/pages/TodoEdit.vue` | 重写 |
| `src/pages/WorkEdit.vue` | 重写 |
| `src/pages/Work.vue` | 修改 |
| `src/pages/Expense.vue` | 修改 |
| `src/pages/Import.vue` | 修改 |
| `src/components/ui/AppAvatar.vue` | 修改 |
| `src/components/todo/TodoEditor.vue` | 重写 |
| `src/components/todo/TodoCard.vue` | 重写 |
| `src/components/work/WorkEditor.vue` | 重写 |
| `src/components/work/WorkCard.vue` | 重写 |
| `src/components/schedule/ScheduleWeekView.vue` | 重写 |
| `src/components/schedule/ScheduleEditor.vue` | 重写 |
| `src/layouts/MobileLayout.vue` | 重写 |

## 数据库变更

需要运行SQL增加以下表/字段：

```sql
-- 1. memory_photos 表（大事记照片）
CREATE TABLE IF NOT EXISTS memory_photos (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  memory_id UUID NOT NULL REFERENCES memories(id) ON DELETE CASCADE,
  storage_path TEXT NOT NULL,
  url TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. todos 表新增字段
ALTER TABLE todos ADD COLUMN IF NOT EXISTS deadline_date DATE;
ALTER TABLE todos ADD COLUMN IF NOT EXISTS deadline_time TIME;

-- 3. Storage bucket 确保 public
-- photos bucket 应设置为 public
```

## 剩余问题

无。所有8项修复全部完成，TypeScript 编译无错误，Vite 构建成功。
